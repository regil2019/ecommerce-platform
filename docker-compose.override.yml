# =============================================================================
# docker-compose.override.yml
# Este ficheiro é carregado automaticamente com o docker-compose.yml.
# Override vazio — todos os serviços iniciam com: docker compose up -d
# =============================================================================
